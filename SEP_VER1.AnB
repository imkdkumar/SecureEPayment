
Protocol: SEP_VER1

Types:
Agent C, M, PG;
Number OD, PRICE;
Function sk,pk,card_number,secretk;

Knowledge:

C: M,C,PG,pk(M),pk(C),pk(PG),inv(pk(C)), card_number(C);
M: C,M,PG,pk(M),pk(C),pk(PG),inv(pk(M));
PG: C,M,PG,pk(M),pk(C),pk(PG),inv(pk(PG));

Actions:

C->M : {C,M,OD}pk(M) 
M->C : {C,M,OD,PRICE}pk(C)

Goals:

C authenticates M on OD
